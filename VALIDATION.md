# 交付检查记录

日期：2026-09-14。对象：本地初始化包，不是个人学习验收或真实行情研究。

| 检查 | 实际结果 |
|---|---|
| `python3 scripts/validate.py` | 通过：8 阶段、52 相对周、61 任务规范、32 来源记录；本地 Markdown 链接存在 |
| `python3 -m unittest discover -s tests -v` | 57 项通过 |
| 三个 `examples/*.py` | 均成功运行；只使用合成数据 |
| `publish.py` / `sync_issues.py` / `sync_project.py` 默认模式 | 离线预览通过，不要求本机有 gh 或联网 |
| Issues 模拟 API | 创建、重跑防重复、保留已关闭状态和人工正文通过 |
| Project 模拟 API | 创建/添加/补空字段、重跑防重复、保留状态/归档、拒绝错误 Project 编号通过 |
| GitHub 仓库文件 | 本次以保留初始提交的方式提交；以 GitHub 提交记录为准 |
| 实际 Issues/Milestones/Project 批量写入 | **未执行**；不可把任务定义或模拟测试当作远端创建成功 |
| 本人知识掌握与策略效果 | **未验收**；不得据本记录标记课程/策略完成 |

## 合成示例观察

账本示例中，近月 100→101、远月 111→112，假设乘数 10、各持有一手、不计费用，真实合约级累计毛盈亏为 20；把跨合约拼接价格 100→112 直接当收益会得到错误的 120。它不实现完整逐日结算系统，也不是任何真实品种参数。

时点示例中，9 月 2 日只能使用已经发布并收到的初值 100，而不能使用 9 月 8 日才收到的修订值 110。历史发布时间和历史接收时间在例子里均人为明确给定，不声称真实数据商提供了这些字段。

选择偏差示例使用固定随机种子 20260914、每段 120 个 IID 噪声观测、30 次重复。1/10/100 个候选的被选样本内标准化均值分别约为 0.004/1.679/2.448；对应独立样本外约为 -0.130/0.224/0.112。这不是年化 Sharpe、PBO/DSR 实现或市场收益预测，不从单次种子推导统计保证。

## 仍然需要本人核验

真实 GitHub CLI/API 权限、Issues/Milestones 及 Project 的实际创建；动态合约规则和程序化交易要求；数据授权、发布时间/修订历史；真实研究实验的可复现性与本人独立解释能力。模拟测试不能替代这些步骤。
